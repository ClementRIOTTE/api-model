import pandas as pd

from train import clean_data
class TestTrain(TestCase)
    def test_clean_data(self):
        'survived' : 

    result_df = clean-data(df)

self.assertEqual(result_df.shape[0],)
self.assertEqual(result_df['sex'],)